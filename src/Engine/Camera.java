package Engine;

import org.joml.*;

import java.lang.Math;

public class Camera {
    private Position position = new Position(0, 0, 0);

    public Position getPosition() {
        return position;
    }
}